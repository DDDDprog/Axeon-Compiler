// ZeoC test - multi-line features
import std.io

// Struct with new syntax
struct Point {
    x: int
    y: int
}

// Function with new fn syntax
fn main() -> int {
    let x: int = 5
    let y: int = 10
    print(x)
    print(y)
    
    return x + y
}
