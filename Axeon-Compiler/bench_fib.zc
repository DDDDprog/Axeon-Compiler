import std.io

fn fib(n: int) -> int {
    if (n <= 1) {
        return n
    }
    return fib(n - 1) + fib(n - 2)
}

fn main() -> int {
    let i: int = 0
    let result: int = 0
    
    while (i < 20) {
        result = fib(i)
        i = i + 1
    }
    
    print(result)
    return 0
}
